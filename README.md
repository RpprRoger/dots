dots plugin
=========
# options
$(element).dots({
    char: '.',
    count: 3,
    speed: 500,
    start: true
});

# start
$(element).dots(true);
# stop
$(element).dots(false);
# Update options
$(element).dots({
    char: '.',
    count: 3,
    speed: 500
});
